<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/user.css">

    <title>Edit Show - {{show.title}}</title>
</head>
<body>

<h1>Edit Show - {{show.title}}</h1>

<form action="/admin/edit-show/{{show._id}}" method="POST">
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="{{show.title}}" required><br><br>

    <label for="description">Description:</label>
    <textarea id="description" name="description" required>{{show.description}}</textarea><br><br>

    <label for="cast">Cast:</label>
    <input type="text" id="cast" name="cast" value="{{show.cast}}" required><br><br>

    <label for="rating">Rating:</label>
    <input type="text" id="rating" name="rating" value="{{show.rating}}" required><br><br>

    <label for="duration">Duration:</label>
    <input type="text" id="duration" name="duration" value="{{show.duration}}" required><br><br>

    <label for="listed_in">Listed In:</label>
    <input type="text" id="listed_in" name="listed_in" value="{{show.listed_in}}" required><br><br>

    <button type="submit">Update Show</button>
</form>

<a href="/admin/shows">
    <button>Back to Shows List</button>
</a>

</body>
</html>
