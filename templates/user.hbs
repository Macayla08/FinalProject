<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="user.css">

    <title>User Profile</title>

       
</head>
<body>
    <h1>User Profile</h1>
    <div class="profile-info">
        <p><strong>Username:</strong> {{username}}</p>
        <p><strong>Email:</strong> {{email}}</p>
        <p><strong>Full Name:</strong> {{fullName}}</p>
        <p> <strong>Bio: </strong>{{bio}}</p>
        <p> <strong>Age: </strong>{{age}}</p>


    </div>

    <div class="form-container">
            <form action="/update-profile" method="POST" id="profileForm">
                <div class="column-left">
                    <label for="name">Name:</label>
                    <input type="text" name="name" id="name" placeholder={{fullName}}>
        
                    <label for="email">Email:</label>
                    <input type="email" name="email" id="email" placeholder={{email}}>

                    <label for="gender">Gender:</label>
                    <select name="gender" id="gender">
                        <option value="male">Male</option>
                        <option value="female">Female</option>
                        <option value="other">Other</option>
                    </select>

                    <label for="age">Age</label>
                    <input type="number" name="age" id="age" placeholder={{age}}>
                    <label for="bio">Bio</label>
                    <input type="text" name="bio" id="bio" placeholder={{bio}}>

                <button type="submit">Submit</button>
            </form>
        
    </div>
    <div>
        <form action="/delete" method="POST" id="deleteForm" onsubmit="return confirm('Proceeding will delete your profile permanently')">
            <button class='delete'type="submit">Delete Profile</button>
        </form>
    </div>

     
        
</body>
</html>
